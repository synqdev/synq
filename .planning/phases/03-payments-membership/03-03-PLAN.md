---
phase: 03-payments-membership
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - app/api/admin/membership-plans/route.ts
  - app/api/admin/membership-plans/[id]/route.ts
  - app/[locale]/(admin)/admin/memberships/page.tsx
  - app/[locale]/(admin)/admin/memberships/membership-plan-table.tsx
  - app/[locale]/(admin)/admin/memberships/membership-plan-form.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can view all membership plans in a table"
    - "Admin can create a new session pack plan with Stripe product and one-time price"
    - "Admin can edit an existing plan's display details"
    - "Admin can deactivate a membership plan (soft delete)"
    - "Stripe product/price creation and DB write are in try/catch with logged compensation on partial failure"
  artifacts:
    - path: "app/api/admin/membership-plans/route.ts"
      provides: "GET list and POST create for membership plans"
      exports: ["GET", "POST"]
    - path: "app/api/admin/membership-plans/[id]/route.ts"
      provides: "PUT update and DELETE deactivate for membership plans"
      exports: ["PUT", "DELETE"]
    - path: "app/[locale]/(admin)/admin/memberships/page.tsx"
      provides: "Admin membership plans management page"
    - path: "app/[locale]/(admin)/admin/memberships/membership-plan-table.tsx"
      provides: "Client component table for displaying and managing plans"
    - path: "app/[locale]/(admin)/admin/memberships/membership-plan-form.tsx"
      provides: "Client component form for creating/editing plans"
  key_links:
    - from: "app/[locale]/(admin)/admin/memberships/membership-plan-table.tsx"
      to: "/api/admin/membership-plans"
      via: "fetch for CRUD operations"
      pattern: "fetch.*admin/membership-plans"
    - from: "app/api/admin/membership-plans/route.ts"
      to: "src/lib/services/membership.service.ts"
      via: "service function calls"
      pattern: "getMembershipPlans|createMembershipPlan"
    - from: "app/api/admin/membership-plans/route.ts"
      to: "src/lib/stripe/client.ts"
      via: "Stripe product/price creation"
      pattern: "getStripe\\(\\)"
---

<objective>
Build the admin interface for managing prepaid session pack plans, including Stripe product/price creation and plan CRUD operations.

Purpose: Admin must be able to create and manage session pack plans before customers can purchase them. Plans are created both in the local DB and in Stripe simultaneously.
Output: Admin API routes for plan CRUD, admin page with table and form for plan management.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-payments-membership/03-RESEARCH.md
@.planning/phases/03-payments-membership/03-01-SUMMARY.md
@src/lib/validations/membership.ts
@src/lib/services/membership.service.ts
@app/[locale]/(admin)/admin/services/page.tsx
@app/[locale]/(admin)/admin/services/service-table.tsx
@app/[locale]/(admin)/admin/services/service-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin membership plan API routes</name>
  <files>
    app/api/admin/membership-plans/route.ts
    app/api/admin/membership-plans/[id]/route.ts
  </files>
  <action>
    1. Create `app/api/admin/membership-plans/route.ts`:

       **GET handler**: List all membership plans
       - Verify admin session via `getAdminSession()` — return 401 if not authenticated
       - Accept optional query param `activeOnly` (defaults to false for admin view)
       - Call `getMembershipPlans(activeOnly)` from membership service
       - Return plans array as JSON

       **POST handler**: Create a new session pack plan
       - Verify admin session
       - Parse and validate request body with `createMembershipPlanSchema` from validations
       - Create Stripe product and one-time price with compensation strategy:
         1. Create Stripe product: `getStripe().products.create({ name, metadata: { sessionsIncluded: String(sessionsIncluded) } })`
         2. Create Stripe price: `getStripe().prices.create({ product: product.id, unit_amount: priceJpy, currency: 'jpy' })` — NO `recurring` field (one-time price)
         3. Create DB record: `createMembershipPlan(data)` with stripeProductId and stripePriceId
         4. If DB write fails after Stripe creation: log error with Stripe IDs for manual cleanup, return 500
       - Return 201 with created plan

    2. Create `app/api/admin/membership-plans/[id]/route.ts`:

       **PUT handler**: Update membership plan
       - Verify admin session
       - Parse and validate with `updateMembershipPlanSchema`
       - Call `updateMembershipPlan(id, data)` — only updates display fields (name, nameEn, description, sessionsIncluded, isActive). Price changes require creating a new plan (Stripe prices are immutable).
       - Return updated plan

       **DELETE handler**: Deactivate membership plan (soft delete per project convention)
       - Verify admin session
       - Call `deactivateMembershipPlan(id)` — returns the plan with stripeProductId
       - Deactivate the Stripe product: `getStripe().products.update(plan.stripeProductId, { active: false })`
       - If Stripe deactivation fails after DB update: log warning but still return success (DB is source of truth, Stripe can be cleaned up later)
       - Return 200 with deactivated plan

    Follow existing admin API patterns from `app/api/admin/customers/route.ts` — admin session check, Zod validation, try/catch with proper HTTP status codes.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - GET route returns plans list
    - POST route creates Stripe product/price (one-time, no recurring) and DB record
    - PUT route updates plan display fields
    - DELETE route soft-deletes plan and deactivates in Stripe
    - Partial failure between Stripe and DB is logged with IDs for recovery
  </verify>
  <done>Admin membership plan API routes support full CRUD with Stripe integration. One-time prices (no recurring). Partial failure handling with logged Stripe IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Create admin membership plans management page</name>
  <files>
    app/[locale]/(admin)/admin/memberships/page.tsx
    app/[locale]/(admin)/admin/memberships/membership-plan-table.tsx
    app/[locale]/(admin)/admin/memberships/membership-plan-form.tsx
  </files>
  <action>
    1. Create `app/[locale]/(admin)/admin/memberships/page.tsx`:
       - Server component with admin session verification (redirect to login if not authenticated)
       - Fetch membership plans from DB directly via Prisma (server component — no API call needed)
       - Render page title and MembershipPlanTable client component with initial data
       - Follow pattern from `app/[locale]/(admin)/admin/services/page.tsx`

    2. Create `app/[locale]/(admin)/admin/memberships/membership-plan-table.tsx`:
       - `'use client'` component
       - Display plans in a table with columns: Name, Price (format as ¥X,XXX), Sessions (X回), Status (active/inactive badge), Actions
       - "Create Plan" button at top opens inline form (same pattern as service-table.tsx)
       - Each row has Edit and Deactivate buttons
       - Edit replaces row with inline form (project convention: inline editing, not modal)
       - Deactivate sends DELETE to `/api/admin/membership-plans/[id]`
       - Use `router.refresh()` after mutations to reload data

    3. Create `app/[locale]/(admin)/admin/memberships/membership-plan-form.tsx`:
       - `'use client'` component for create/edit form
       - Fields: name (text, required), nameEn (text, optional), description (textarea, optional), priceJpy (number, required), sessionsIncluded (number, required, e.g. 5, 10, 20)
       - For create: POST to `/api/admin/membership-plans`
       - For edit: PUT to `/api/admin/membership-plans/[id]` — price field is read-only when editing (Stripe prices are immutable)
       - Cancel button to close form
       - Use fetch with loading state
       - Style with Tailwind, follow existing form patterns from `service-form.tsx`
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Page renders at `/admin/memberships`
    - Table displays plan data with proper JPY formatting and sessions count (X回)
    - Create form submits to API and creates plan
    - Edit form updates plan display fields
    - Deactivate button soft-deletes plan
  </verify>
  <done>Admin can view session pack plans table, create new plans (with Stripe product/price), edit display details, and deactivate plans from the memberships management page</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — all files compile
- Admin memberships page loads at `/{locale}/admin/memberships`
- Plan table shows name, price, sessions count, status columns
- Create flow: fills form -> POST to API -> Stripe product + one-time price created -> DB record created -> table refreshes
- Edit flow: click edit -> inline form -> PUT to API -> DB updated -> table refreshes
- Deactivate: click deactivate -> DELETE to API -> isActive=false in DB + Stripe -> table refreshes
- Partial failure between Stripe and DB is handled with logging
</verification>

<success_criteria>
- Admin membership plans page exists at `/admin/memberships`
- Admin can create session pack plans (form creates Stripe product + one-time price + DB record)
- Admin can edit plan display details (name, description, sessions — not price)
- Admin can deactivate plans (soft delete in DB + Stripe product deactivation)
- JPY prices displayed with yen formatting
- Sessions displayed as "X回"
- All routes protected by admin session verification
- `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-payments-membership/03-03-SUMMARY.md`
</output>
