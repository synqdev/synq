---
phase: 02-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
autonomous: true

must_haves:
  truths:
    - "Customer has assigned staff support for CRM ownership workflows"
    - "Customer has visit and balance fields needed by list/detail/reporting views"
    - "Schema changes are strictly limited to fields required by immediate next tasks"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Atomic Phase 2 schema delta for customer CRM"
      contains: "assignedStaffId, visitCount, lastVisitDate, outstandingAmount"
---

<objective>
Apply the minimum schema change required to start Phase 2 customer CRM work.

Purpose: Keep task/PR scope tight for automated review quality.
Output: Prisma schema updated with only customer ownership/metrics fields and relation.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add customer ownership and metric fields only</name>
  <files>prisma/schema.prisma</files>
  <action>
Add only these schema changes:

1. In `Customer`:
- `assignedStaffId String?`
- `visitCount Int @default(0)`
- `lastVisitDate DateTime?`
- `outstandingAmount Int @default(0)`
- relation: `assignedStaff Worker? @relation("CustomerAssignedStaff", fields: [assignedStaffId], references: [id], onDelete: SetNull)`
- index: `@@index([assignedStaffId])`

2. In `Worker`:
- reverse relation: `assignedCustomers Customer[] @relation("CustomerAssignedStaff")`

Do not include other Phase 2 models in this plan. Those move to later plan-specific PRs.
  </action>
  <verify>
Run:
- `npx prisma validate`
- `npx prisma format`
- `npx prisma generate`
All commands must pass.
  </verify>
  <done>Schema validates and generates. Diff only contains customer ownership/metrics fields and worker reverse relation.</done>
</task>

</tasks>

<verification>
1. Prisma schema validates successfully
2. Prisma client generates successfully
3. No unrelated schema entities added in this PR
</verification>

<success_criteria>
- Customer has ownership + metric fields needed for CRM list/detail
- Worker relation to assigned customers exists
- PR scope remains atomic for automated review
</success_criteria>

<output>
After completion, create `.planning/phases/02-parity/02-01-SUMMARY.md`
</output>
