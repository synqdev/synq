---
phase: 03-payments-membership
plan: 06
type: execute
wave: 5
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - app/api/admin/payments/route.ts
  - app/[locale]/(admin)/admin/customers/[id]/page.tsx
  - app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx
  - app/[locale]/(admin)/admin-nav.tsx
  - messages/ja.json
  - messages/en.json
autonomous: false

must_haves:
  truths:
    - "Admin can view payment history per customer"
    - "Admin can see session balance on customer detail page"
    - "Admin navigation includes Memberships and Scan tabs"
    - "All new pages have Japanese and English translations"
    - "Full checkout flow works end-to-end: plan selection -> Stripe checkout -> webhook -> sessions added -> QR visible -> scan decrements"
  artifacts:
    - path: "app/api/admin/payments/route.ts"
      provides: "GET endpoint for payment history with customer filter"
      exports: ["GET"]
    - path: "messages/ja.json"
      provides: "Japanese translations for all membership/payment UI"
    - path: "messages/en.json"
      provides: "English translations for all membership/payment UI"
  key_links:
    - from: "app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx"
      to: "prisma"
      via: "membership and payment data included in customer query"
      pattern: "memberships|payments"
    - from: "app/[locale]/(admin)/admin-nav.tsx"
      to: "admin/memberships"
      via: "nav item href"
      pattern: "memberships|scan"
---

<objective>
Complete the admin payment views, update navigation, add i18n translations, and verify the entire phase works end-to-end.

Purpose: This final plan ties everything together: admin can see payment data and session balances, navigation is updated for new sections, all text is localized, and the full flow is verified.
Output: Admin payment API, customer detail with session balance info, updated nav, complete i18n, end-to-end verification.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-payments-membership/03-RESEARCH.md
@.planning/phases/03-payments-membership/03-02-SUMMARY.md
@.planning/phases/03-payments-membership/03-03-SUMMARY.md
@.planning/phases/03-payments-membership/03-04-SUMMARY.md
@.planning/phases/03-payments-membership/03-05-SUMMARY.md
@app/[locale]/(admin)/admin-nav.tsx
@app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx
@messages/ja.json
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin payment API and update customer detail with session info</name>
  <files>
    app/api/admin/payments/route.ts
    app/[locale]/(admin)/admin/customers/[id]/page.tsx
    app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx
  </files>
  <action>
    1. Create `app/api/admin/payments/route.ts`:

       **GET handler**: List payments with optional customer filter
       - Verify admin session — return 401 if not authenticated
       - Accept query params: `customerId` (optional, filter by customer), `page` (default 1), `limit` (default 20)
       - Validate with `paymentQuerySchema` from validations
       - Query Payment from DB with optional customerId filter, include customer relation (name, email), orderBy createdAt desc, paginate with skip/take
       - Return `{ payments, total, page, limit }`

    2. Update `app/[locale]/(admin)/admin/customers/[id]/page.tsx`:
       - Add membership and payment data to the existing customer detail query
       - Include `memberships: { include: { plan: true }, orderBy: { purchasedAt: 'desc' } }` in Prisma query
       - Include `payments: { orderBy: { createdAt: 'desc' }, take: 10 }` in Prisma query
       - Pass membership and payment data to the CustomerDetail component

    3. Update `app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx`:
       - Add a "Session Packs" section below existing customer info:
         - If customer has active membership(s): show plan name, sessions remaining / sessions included (e.g. "3/5回"), status badge (green for active, gray for exhausted), purchase date
         - List all memberships (not just active)
         - If no memberships: show "未購入" / "No purchases" text
       - Add a "Payment History" section:
         - Table with columns: Date, Amount (JPY), Status (badge), Description
         - Show last 10 payments
       - Format amounts with yen symbol and comma grouping
       - Format dates in Japanese locale format
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Payment API returns paginated payments at `/api/admin/payments`
    - Customer detail page shows session pack section with remaining/total counts
    - Customer detail page shows payment history table
  </verify>
  <done>Admin can view payment history via API, customer detail page shows session balances and recent payments</done>
</task>

<task type="auto">
  <name>Task 2: Update admin navigation and add i18n translations</name>
  <files>
    app/[locale]/(admin)/admin-nav.tsx
    messages/ja.json
    messages/en.json
  </files>
  <action>
    1. Update `app/[locale]/(admin)/admin-nav.tsx`:
       - Add two new nav items to the `navItems` array:
         - `{ href: '/admin/memberships', labelKey: 'memberships' }` (after customers)
         - `{ href: '/admin/scan', labelKey: 'scan' }` (at end)
       - Keep existing nav items in their current order

    2. Update `messages/ja.json` — add new keys:
       ```
       "admin.nav.memberships": "回数券"
       "admin.nav.scan": "QRスキャン"
       "membership.title": "回数券プラン"
       "membership.purchase": "購入する"
       "membership.sessions": "回数"
       "membership.sessionsRemaining": "残り回数"
       "membership.sessionsFormat": "{count}回"
       "membership.active": "有効"
       "membership.exhausted": "使い切り"
       "membership.noSessions": "残りセッションなし"
       "membership.purchaseMore": "追加購入"
       "membership.noPurchases": "未購入"
       "membership.success.title": "購入が完了しました"
       "membership.success.message": "セッションが追加されました。アカウントページでQRコードをご確認いただけます。"
       "membership.cancel.title": "お支払いがキャンセルされました"
       "membership.cancel.message": "お支払いが完了しませんでした。もう一度お試しください。"
       "membership.cancel.retry": "プランを選び直す"
       "account.title": "マイアカウント"
       "account.qr.title": "会員QRコード"
       "account.qr.refresh": "QRコードを更新"
       "account.qr.expires": "24時間で有効期限切れ — 更新してください"
       "scan.title": "QRスキャン"
       "scan.scanning": "スキャン中..."
       "scan.scanAgain": "もう一度スキャン"
       "scan.valid": "有効"
       "scan.invalid": "無効なQRコード"
       "scan.expired": "QRコードの有効期限切れ"
       "scan.visitRecorded": "来店記録済み"
       "scan.cameraPermission": "カメラのアクセスを許可してください"
       "payment.history": "お支払い履歴"
       "payment.amount": "金額"
       "payment.status": "ステータス"
       "payment.date": "日付"
       "payment.succeeded": "成功"
       "payment.failed": "失敗"
       "payment.pending": "処理中"
       ```

    3. Update `messages/en.json` — add corresponding English keys:
       ```
       "admin.nav.memberships": "Session Packs"
       "admin.nav.scan": "QR Scan"
       "membership.title": "Session Packs"
       "membership.purchase": "Purchase"
       "membership.sessions": "Sessions"
       "membership.sessionsRemaining": "Sessions Remaining"
       "membership.sessionsFormat": "{count} sessions"
       "membership.active": "Active"
       "membership.exhausted": "Exhausted"
       "membership.noSessions": "No sessions remaining"
       "membership.purchaseMore": "Purchase More"
       "membership.noPurchases": "No purchases"
       "membership.success.title": "Purchase Complete"
       "membership.success.message": "Your sessions have been added. View your QR code on your account page."
       "membership.cancel.title": "Payment Cancelled"
       "membership.cancel.message": "Your payment was not completed. Please try again."
       "membership.cancel.retry": "Choose a plan"
       "account.title": "My Account"
       "account.qr.title": "Membership QR Code"
       "account.qr.refresh": "Refresh QR Code"
       "account.qr.expires": "Expires in 24 hours — refresh to regenerate"
       "scan.title": "QR Scan"
       "scan.scanning": "Scanning..."
       "scan.scanAgain": "Scan Again"
       "scan.valid": "Valid"
       "scan.invalid": "Invalid QR Code"
       "scan.expired": "QR Code Expired"
       "scan.visitRecorded": "Visit Recorded"
       "scan.cameraPermission": "Please allow camera access"
       "payment.history": "Payment History"
       "payment.amount": "Amount"
       "payment.status": "Status"
       "payment.date": "Date"
       "payment.succeeded": "Succeeded"
       "payment.failed": "Failed"
       "payment.pending": "Pending"
       ```

    Add these keys to the existing JSON structures. Do NOT overwrite existing keys. Merge into the appropriate nested structure that already exists in each file.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Admin nav shows Session Packs and QR Scan tabs
    - Both ja.json and en.json have all new keys
    - `npm run build` succeeds (i18n keys valid)
  </verify>
  <done>Admin navigation includes Session Packs and QR Scan tabs. All session pack, payment, account, and scan UI text translated to Japanese and English.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end verification of payment and session redemption flow</name>
  <files>none (verification only)</files>
  <action>
    Human verifies the complete end-to-end flow across all plans in this phase. This checkpoint validates that all components (schema, webhook, checkout, QR, admin views, i18n) work together correctly.
  </action>
  <verify>
    1. Start dev server: `npm run dev`
    2. In a separate terminal, start Stripe webhook forwarding: `stripe listen --forward-to localhost:3000/api/stripe/webhook`
    3. Admin flow:
       a. Navigate to `http://localhost:3000/ja/admin/memberships`
       b. Verify "回数券" tab appears in admin nav
       c. Create a new session pack plan (name: "5回プラン", price: 5000, sessions: 5)
       d. Verify plan appears in table with "5回" and "¥5,000"
    4. Customer flow:
       a. Navigate to `http://localhost:3000/ja/membership`
       b. Verify plan cards display with session count and pricing
       c. Click "購入する" on the 5-session plan
       d. Complete Stripe test checkout (use card 4242 4242 4242 4242)
       e. Verify redirect to success page with "5回のセッションが追加されました"
    5. Webhook verification:
       a. Check Stripe CLI output shows webhook received
       b. Verify customer record updated with stripeCustomerId
       c. Verify Membership record created with sessionsRemaining: 5 and status: 'active'
       d. Verify Payment record created with status: 'succeeded'
    6. QR code flow:
       a. Navigate to `http://localhost:3000/ja/account`
       b. Verify QR code displays with "残り 5回"
       c. Navigate to `http://localhost:3000/ja/admin/scan`
       d. Verify "QRスキャン" tab appears in admin nav
       e. (If camera available) Scan the QR code
       f. Verify success shows "残り 4/5回" (4 remaining after decrement)
    7. Admin customer detail:
       a. Navigate to the customer's detail page in admin
       b. Verify session pack section shows "4/5回" remaining
       c. Verify payment history section shows the payment
  </verify>
  <done>Full end-to-end flow verified: plan creation -> Stripe one-time checkout -> webhook processing -> sessions added -> QR code visible -> staff scan decrements session -> admin sees session balance and payment history</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — all files compile
- `npm run build` — production build succeeds
- Admin nav has Session Packs and QR Scan items
- Admin payments API returns filtered, paginated results
- Customer detail page shows session balances and payment info
- All i18n keys present in both ja.json and en.json
- End-to-end flow works: plan creation -> checkout -> webhook -> sessions added -> QR code -> scan decrements
</verification>

<success_criteria>
- Admin can view payment history per customer on detail page
- Admin can see session balance (remaining/total) on customer detail
- Admin navigation includes Session Packs and QR Scan tabs
- All UI text is translated to Japanese and English
- Full end-to-end flow verified by human tester
- `npm run build` succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-payments-membership/03-06-SUMMARY.md`
</output>
