---
phase: 07-appointment-view-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/[locale]/(admin)/admin/settings/page.tsx
  - src/components/admin/SettingsForm.tsx
  - messages/en.json
  - messages/ja.json
autonomous: true

must_haves:
  truths:
    - "Admin settings page exists at /{locale}/admin/settings with auth protection"
    - "Form shows AI provider, business type, auto-transcribe, recording language, audio quality fields"
    - "Saving settings persists to database via PUT /api/admin/settings"
    - "Success feedback shown after saving (toast or inline message)"
    - "Form loads current settings from GET /api/admin/settings on mount"
  artifacts:
    - path: "app/[locale]/(admin)/admin/settings/page.tsx"
      provides: "Server component with auth check rendering SettingsForm"
      min_lines: 15
    - path: "src/components/admin/SettingsForm.tsx"
      provides: "Client form component with SWR fetch, validation, and save"
      min_lines: 60
  key_links:
    - from: "src/components/admin/SettingsForm.tsx"
      to: "/api/admin/settings"
      via: "SWR GET + fetch PUT for settings CRUD"
      pattern: "useSWR.*admin/settings"
    - from: "src/components/admin/SettingsForm.tsx"
      to: "/api/admin/settings"
      via: "PUT request to save settings"
      pattern: "fetch.*admin/settings.*PUT"
---

<objective>
Create the global admin settings page at `/{locale}/admin/settings` with a form for AI provider selection, business type, recording preferences (auto-transcribe, language, audio quality).

Purpose: Allows shop owners to configure AI and recording defaults that apply to all appointments.
Output: Settings server page + SettingsForm client component.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-appointment-view-dashboard/07-CONTEXT.md
@.planning/phases/07-appointment-view-dashboard/07-01-SUMMARY.md
@app/[locale]/(admin)/layout.tsx
@app/api/admin/settings/route.ts
@src/components/ui/card.tsx
@src/components/ui/button.tsx
@src/components/ui/select.tsx
@messages/en.json
@messages/ja.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add save/saved keys to admin.settingsPage i18n namespace</name>
  <files>
    messages/en.json
    messages/ja.json
  </files>
  <action>
1. In `messages/en.json`, find the `"settingsPage"` object inside `"admin"`. Add two keys at the end of the object (before the closing brace):
   ```json
   "save": "Save",
   "saved": "Saved"
   ```
   The existing keys are: title, loadError, aiProvider, businessType, recordingLang, audioQuality, autoTranscribe, general, salon, clinic, standard, high. Add save and saved after high.

2. In `messages/ja.json`, find the same `"settingsPage"` object. Add:
   ```json
   "save": "保存",
   "saved": "保存しました"
   ```

   NOTE: 07-01 deviated from the original plan by using `admin.settingsPage` instead of `admin.settings` to avoid collision with the existing `admin.settings` string value. This plan uses `admin.settingsPage` accordingly.
  </action>
  <verify>
    `npm run build` succeeds. JSON files parse without errors.
  </verify>
  <done>
    admin.settingsPage namespace has save and saved keys in both locale files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings page and form component</name>
  <files>
    app/[locale]/(admin)/admin/settings/page.tsx
    src/components/admin/SettingsForm.tsx
  </files>
  <action>
1. Create `app/[locale]/(admin)/admin/settings/page.tsx`:
   - Server component
   - Auth check: `const isAdmin = await getAdminSession()`, redirect to `/${locale}/admin/login` if false
   - Extract locale from params
   - Render SettingsForm with locale prop
   - This page is inside the (admin) route group so it inherits the admin layout automatically

2. Create `src/components/admin/SettingsForm.tsx`:
   - 'use client' component
   - Props: locale (string)
   - Use SWR to fetch current settings from `/api/admin/settings` with { revalidateOnFocus: false }
   - Form state managed with useState, initialized from SWR data via useEffect (when data loads, set form values)
   - Show Spinner while loading

   IMPORTANT: Use `useTranslations('admin.settingsPage')` -- NOT `admin.settings`. The `admin.settings` key is a plain string value ("Settings"), not a namespace. 07-01 deviated to `admin.settingsPage` to avoid this collision.

   Form sections (use Card component for visual grouping):

   a. **AI Provider** section:
      - Label from `admin.settingsPage.aiProvider` (accessed as `t('aiProvider')` since useTranslations is scoped to `admin.settingsPage`)
      - Select dropdown with options: "openai" (OpenAI), "gemini" (Google Gemini), "anthropic" (Anthropic)
      - Controlled by form state `aiProvider`

   b. **Business Type** section:
      - Label from `t('businessType')`
      - Select dropdown with options using i18n keys:
        - "general" -> `t('general')`
        - "salon" -> `t('salon')`
        - "clinic" -> `t('clinic')`
      - Controlled by form state `businessType`

   c. **Recording Preferences** section:
      - Auto-transcribe toggle: checkbox input with label from `t('autoTranscribe')`, controlled by `autoTranscribe` boolean
      - Recording language: Select with options "ja" (Japanese), "en" (English), label from `t('recordingLang')`, controlled by `recordingLang`
      - Audio quality: Select with options using i18n keys:
        - "standard" -> `t('standard')`
        - "high" -> `t('high')`
      - Label from `t('audioQuality')`, controlled by `audioQuality`

   d. **Save button**:
      - Button with label from `t('save')`
      - onClick: async handler that does `fetch('/api/admin/settings', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(formValues) })`
      - Show loading state on button during save (disabled + spinner or "Saving...")
      - On success: show success message inline (a green text below the button with `t('saved')` i18n key, auto-hide after 3 seconds via setTimeout)
      - On error: show error message inline (red text)
      - After successful save, call SWR mutate to refresh cached data

   Styling:
   - Page title: `<h1 className="text-2xl font-bold text-gray-900 mb-6">{t('title')}</h1>`
   - Use Card components for each section with appropriate padding
   - Form fields use consistent label + input pattern: label in text-sm font-medium text-gray-700, inputs with standard form styling
   - For select elements, use the existing Select component from `@/components/ui/select` if it fits, otherwise use native `<select>` with Tailwind styling (border rounded-md px-3 py-2 text-sm)
   - Max width container: `max-w-2xl mx-auto p-6`
  </action>
  <verify>
    `npm run build` succeeds.
    Navigate to `/{locale}/admin/settings` when logged in -- form loads with current settings from API.
    Change a setting, click Save -- success message appears, refreshing page shows persisted value.
    Navigate to `/{locale}/admin/settings` when NOT logged in -- redirects to login.
  </verify>
  <done>
    Settings page accessible at `/{locale}/admin/settings` with auth protection. Form loads current settings via SWR, allows editing AI provider, business type, and recording preferences. Save persists to database via PUT API and shows success feedback. All i18n keys use admin.settingsPage namespace.
  </done>
</task>

</tasks>

<verification>
- Settings page loads at `/{locale}/admin/settings` with auth protection
- Form displays all five configurable fields with current values from database
- Saving settings persists changes and shows success feedback
- Form validates correctly (Select dropdowns constrain values)
- Page inherits admin layout (header, navigation)
- All i18n references use admin.settingsPage (NOT admin.settings)
- `npm run build` succeeds
</verification>

<success_criteria>
- Admin settings page exists and is auth-protected
- All five settings fields are editable (AI provider, business type, auto-transcribe, recording language, audio quality)
- Settings persist to database via API
- Success/error feedback on save
- Settings load from database on page visit
- useTranslations('admin.settingsPage') used throughout (not 'admin.settings')
</success_criteria>

<output>
After completion, create `.planning/phases/07-appointment-view-dashboard/07-04-SUMMARY.md`
</output>
