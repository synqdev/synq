---
phase: 06-ask-ai-chat
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx
  - messages/ja.json
  - messages/en.json
autonomous: false

must_haves:
  truths:
    - "Opening chat from customer detail page auto-sets customer context"
    - "Full flow works: open panel, send message, see streaming response"
  artifacts:
    - path: "app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx"
      provides: "Chat button that opens panel with customer context"
      contains: "useChatContext"
  key_links:
    - from: "app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx"
      to: "src/components/chat/ChatProvider.tsx"
      via: "useChatContext to set customerId and open panel"
      pattern: "setCustomerId|setIsOpen"
---

<objective>
Wire customer-scoped chat entry point and verify the complete end-to-end flow with human testing.

Purpose: Connects the chat panel to customer context and validates the full feature works as expected.
Output: Customer detail page has "Ask AI" button, verified working flow
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ask-ai-chat/06-CONTEXT.md
@.planning/phases/06-ask-ai-chat/06-01-SUMMARY.md
@.planning/phases/06-ask-ai-chat/06-02-SUMMARY.md
@app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Customer detail chat integration</name>
  <files>
    app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx
    messages/ja.json
    messages/en.json
  </files>
  <action>
Add an "Ask AI" / "AIに質問" button to the customer detail page that opens the chat panel with the customer's ID pre-set.

- Import `useChatContext` from `@/components/chat`
- Add a button (use existing Button component) in the customer detail header area
- On click: `setCustomerId(customer.id)` then `setIsOpen(true)`
- Use `useTranslations` for the button label
- Style: secondary variant or outline, with a chat/sparkle icon if available
- Add the i18n key `admin.customerDetail.askAi` to ja.json ("AIに質問") and en.json ("Ask AI")
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Customer detail page has Ask AI button
    - Button uses useChatContext to set customer ID and open panel
  </verify>
  <done>
    - Customer detail page has "Ask AI" button that opens chat panel with customer context
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify complete AI chat flow</name>
  <files>
    app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx
  </files>
  <action>
Human verification checkpoint. Present the following verification steps to the user and wait for approval.

What was built: Complete AI chat feature including slide-over panel with streaming responses, customer-scoped context, quick actions, citation links, chat history persistence, and i18n support.

How to verify:
1. Start dev server: `npm run dev`
2. Log in as admin, navigate to any page
3. Verify floating chat button appears (bottom-right corner)
4. Click chat button - panel should slide in from right
5. Type a message and press Enter - verify streaming response appears word-by-word
6. Close panel, reopen - verify previous messages are still visible (persistence)
7. Navigate to a customer detail page
8. Click "Ask AI" button on the customer detail page
9. Verify chat opens with customer context (panel should show customer name)
10. Try a quick action button like "Summarize last visit"
11. Verify quick actions change between customer-scoped and global mode
12. If a response includes karute references, verify citation links are clickable
13. Switch locale to English - verify all labels switch
14. Close panel by clicking backdrop overlay

Wait for user to type "approved" or describe issues before proceeding.
  </action>
  <verify>User confirms the chat feature works as expected</verify>
  <done>User has approved the complete AI chat flow</done>
</task>

</tasks>

<verification>
- Chat panel accessible from all admin pages via floating button
- Customer detail page has dedicated "Ask AI" entry point
- Streaming responses work end-to-end
- Chat history persists and loads on panel reopen
- i18n works in both ja and en
</verification>

<success_criteria>
- Staff can open AI chat from floating button (global) and customer detail (customer-scoped)
- Messages stream in real-time
- Quick actions are context-aware
- Chat history persists per customer
- All success criteria from ROADMAP Phase 6 met
</success_criteria>

<output>
After completion, create `.planning/phases/06-ask-ai-chat/06-03-SUMMARY.md`
</output>
