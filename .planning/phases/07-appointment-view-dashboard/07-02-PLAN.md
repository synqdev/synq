---
phase: 07-appointment-view-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/[locale]/appointment/[id]/layout.tsx
  - app/[locale]/appointment/[id]/page.tsx
  - app/[locale]/appointment/[id]/appointment-workstation.tsx
  - src/components/appointment/AppointmentSidebar.tsx
  - src/components/appointment/AppointmentSummaryBar.tsx
  - src/components/appointment/CustomerInfoPanel.tsx
  - middleware.ts
autonomous: true

must_haves:
  truths:
    - "User can navigate to /{locale}/appointment/{id} and see appointment details"
    - "Appointment page has dark navy sidebar with tab shortcuts, AI chat trigger, prev/next nav"
    - "Four tabs work: Recording, Karute, Customer info, Settings"
    - "Summary bar shows customer name, service, worker, time, status at a glance"
    - "Appointment route is protected by auth (middleware + server component)"
    - "Layout is tablet-first responsive (optimized for iPad)"
  artifacts:
    - path: "app/[locale]/appointment/[id]/page.tsx"
      provides: "Server component with auth check and data loading"
      min_lines: 20
    - path: "app/[locale]/appointment/[id]/appointment-workstation.tsx"
      provides: "Client component with tab switching and layout"
      min_lines: 80
    - path: "app/[locale]/appointment/[id]/layout.tsx"
      provides: "Layout with ChatWrapper for appointment context"
      min_lines: 10
    - path: "src/components/appointment/AppointmentSidebar.tsx"
      provides: "Dark navy sidebar navigation component"
      min_lines: 60
    - path: "src/components/appointment/AppointmentSummaryBar.tsx"
      provides: "Top summary bar with booking details"
      min_lines: 20
    - path: "src/components/appointment/CustomerInfoPanel.tsx"
      provides: "Lightweight customer info for appointment context"
      min_lines: 30
    - path: "middleware.ts"
      provides: "Updated regex to also protect /appointment/ routes"
      contains: "appointment"
  key_links:
    - from: "app/[locale]/appointment/[id]/page.tsx"
      to: "prisma.booking"
      via: "Server-side data fetch with auth"
      pattern: "prisma\\.booking\\.findUnique"
    - from: "app/[locale]/appointment/[id]/appointment-workstation.tsx"
      to: "src/components/recording/RecordingPanel.tsx"
      via: "Recording tab renders RecordingPanel"
      pattern: "RecordingPanel"
    - from: "app/[locale]/appointment/[id]/appointment-workstation.tsx"
      to: "src/components/karute/KaruteEditor.tsx"
      via: "Karute tab renders KaruteEditor"
      pattern: "KaruteEditor"
    - from: "src/components/appointment/AppointmentSidebar.tsx"
      to: "src/components/chat/ChatProvider.tsx"
      via: "AI Chat trigger uses useChatContext"
      pattern: "useChatContext"
---

<objective>
Build the appointment workstation page at `/{locale}/appointment/{id}` with dark navy sidebar navigation, four tabs (Recording, Karute, Customer, Settings), appointment summary bar, and auth protection.

Purpose: This is the main "workstation" practitioners use during appointments -- the core deliverable of Phase 7.
Output: Complete appointment page with sidebar, tabs, and existing component integration.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-appointment-view-dashboard/07-CONTEXT.md
@.planning/phases/07-appointment-view-dashboard/07-RESEARCH.md
@.planning/phases/07-appointment-view-dashboard/07-01-SUMMARY.md
@app/[locale]/(admin)/layout.tsx
@src/components/recording/RecordingPanel.tsx
@src/components/karute/KaruteEditor.tsx
@src/components/chat/ChatProvider.tsx
@middleware.ts
@app/[locale]/(admin)/admin/customers/[id]/customer-detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Appointment page layout, server component, and middleware update</name>
  <files>
    app/[locale]/appointment/[id]/layout.tsx
    app/[locale]/appointment/[id]/page.tsx
    middleware.ts
  </files>
  <action>
1. Create `app/[locale]/appointment/[id]/layout.tsx`:
   - Import ChatWrapper from '@/components/chat/ChatWrapper'
   - Wrap children in a flex container with `h-screen bg-gray-50`
   - Include ChatWrapper so appointment pages have access to chat context
   - This is a simple server component layout (sidebar is in the client workstation because it needs active tab state)

2. Create `app/[locale]/appointment/[id]/page.tsx`:
   - Server component with async params: `{ locale, id }`
   - Auth check: `const isAdmin = await getAdminSession()`, redirect to `/${locale}/admin/login` if false
   - Fetch booking from prisma with findUnique by id, including:
     - customer: { id, name, email, phone, locale, notes, visitCount, lastVisitDate }
     - worker: { id, name, nameEn }
     - service: { id, name, nameEn, duration }
     - karuteRecords: { id, status, createdAt } ordered by createdAt desc
     - recordingSessions: { id, status, startedAt } ordered by createdAt desc
   - Also fetch today's booking IDs for prev/next navigation:
     ```
     const todayBookings = await prisma.booking.findMany({
       where: { startsAt: { gte: startOfDay, lt: endOfDay }, status: { not: 'CANCELLED' } },
       select: { id: true },
       orderBy: { startsAt: 'asc' },
     })
     ```
     Use toZonedTime from '@/lib/utils/time' for startOfDay based on booking.startsAt date.
   - If booking not found, call `notFound()`
   - Render `<AppointmentWorkstation>` passing: booking (serialized -- dates as ISO strings), locale, todayBookingIds (string array)

3. Update `middleware.ts`:
   - Change the admin route regex to ALSO match appointment routes for cookie presence check.
   - Update the existing regex from: `/^\/[a-z]{2}\/admin\/(?!login)/`
   - To: `/^\/[a-z]{2}\/(admin\/(?!login)|appointment\/)/`
   - This ensures appointment routes also require the admin_session cookie. The full JWT verification still happens in the server component.
  </action>
  <verify>
    `npm run build` succeeds without TypeScript errors.
    Visiting `/{locale}/appointment/{valid-booking-id}` when logged in shows the page.
    Visiting `/{locale}/appointment/{id}` when not logged in redirects to admin login.
  </verify>
  <done>
    Appointment page server component loads booking data and renders client workstation. Middleware protects appointment routes. Layout provides ChatWrapper context.
  </done>
</task>

<task type="auto">
  <name>Task 2: Appointment workstation, sidebar, summary bar, and customer info panel</name>
  <files>
    app/[locale]/appointment/[id]/appointment-workstation.tsx
    src/components/appointment/AppointmentSidebar.tsx
    src/components/appointment/AppointmentSummaryBar.tsx
    src/components/appointment/CustomerInfoPanel.tsx
  </files>
  <action>
1. Create `src/components/appointment/AppointmentSummaryBar.tsx`:
   - 'use client' component
   - Props: booking data (customer name, service name, worker name, startsAt, endsAt, status)
   - Horizontal bar with flex-wrap, shows: customer name (font-semibold), service name, worker name, formatted time range (HH:MM - HH:MM), status badge
   - Use useTranslations('admin.appointment') for labels
   - Tailwind: `border-b border-gray-200 bg-white px-4 py-3` with text-sm styling
   - Status badge colors: CONFIRMED=green, PENDING=yellow, CANCELLED=red, NOSHOW=gray

2. Create `src/components/appointment/AppointmentSidebar.tsx`:
   - 'use client' component
   - Props: activeTab (string), onTabChange (fn), bookingId (string), locale (string), todayBookingIds (string[]), customerId (string)
   - Dark navy background: `bg-[#1a2332]`
   - Width: `w-16 md:w-56` (icon-only on mobile, expanded on tablet+)
   - Sidebar sections (top to bottom):
     a. Back to dashboard: Link to `/${locale}/admin/dashboard`, white arrow-left icon, gray-400 hover:white
     b. Tab shortcuts: 4 buttons for recording/karute/customer/settings. Active tab gets `bg-white/10 text-white`, inactive gets `text-gray-400 hover:bg-white/5 hover:text-gray-200`. Use inline SVG icons (mic, clipboard, user, gear). Show label text with `hidden md:inline ml-3`.
     c. AI Chat trigger: Button at mt-auto, calls `useChatContext().setIsOpen(true)` and `setCustomerId(customerId)`. Sparkle/chat SVG icon.
     d. Prev/Next navigation: At bottom. Use Link components. Show chevron-up for previous (if currentIndex > 0), chevron-down for next (if currentIndex < length - 1). Link to `/${locale}/appointment/${todayBookingIds[prevIndex]}`.
   - Use useTranslations('admin.sidebar') for labels

3. Create `src/components/appointment/CustomerInfoPanel.tsx`:
   - 'use client' component
   - Props: customer data (id, name, email, phone, locale, notes, visitCount, lastVisitDate), appLocale (string)
   - Lighter version of CustomerDetail for appointment context -- read-only display, no editing
   - Show: name (h2), email, phone, locale, notes (if any), visit count, last visit date (formatted)
   - Include a Link to full customer detail page: `/${appLocale}/admin/customers/${customer.id}`
   - Use useTranslations('admin.appointment') for customer-related labels
   - Styled with Card-like rounded border + padding

4. Create `app/[locale]/appointment/[id]/appointment-workstation.tsx`:
   - 'use client' component
   - Props: booking (serialized booking data with all relations), locale (string), todayBookingIds (string[])
   - State: activeTab (useState<'recording' | 'karute' | 'customer' | 'settings'>('recording'))
   - Layout: flex h-full
     - Left: AppointmentSidebar (activeTab, onTabChange, bookingId, locale, todayBookingIds, customerId)
     - Right: flex-1 flex-col
       - Top: AppointmentSummaryBar
       - Content: flex-1 overflow-auto p-4 md:p-6
         - Tab content rendered conditionally:
           - 'recording': Render RecordingPanel with customerId, workerId, bookingId, and karuteRecordId (from first karuteRecord if exists)
           - 'karute': If karuteRecords[0] exists, render KaruteEditor with recordId and locale. If no karute, show a message with the i18n key 'noKarute' and optionally a "Create Karute" button (that creates a karute record via server action and refreshes).
           - 'customer': Render CustomerInfoPanel with customer data
           - 'settings': Show brief current settings summary (fetch from /api/admin/settings via SWR). Include a "Manage Settings" link (Link component) to `/${locale}/admin/settings`.
   - Tablet-first responsive: Content area uses reasonable padding, tabs stack correctly on small screens
  </action>
  <verify>
    `npm run build` succeeds.
    Navigate to an appointment page -- sidebar appears with dark navy, tabs switch content, summary bar shows booking details.
    AI Chat button opens chat panel. Prev/Next links navigate between today's appointments.
  </verify>
  <done>
    Complete appointment workstation with dark navy sidebar, four working tabs, summary bar, customer info panel. Sidebar has all required links: back to dashboard, tab shortcuts, AI chat trigger, prev/next navigation. Tablet-first responsive design.
  </done>
</task>

</tasks>

<verification>
- Appointment page loads at `/{locale}/appointment/{id}` with auth protection
- Dark navy sidebar renders with all required navigation elements
- All four tabs display correct content (Recording, Karute, Customer, Settings)
- Summary bar shows customer, service, worker, time, and status
- Sidebar prev/next navigation works between today's appointments
- AI Chat trigger opens chat panel scoped to the appointment's customer
- Page is responsive and tablet-optimized
</verification>

<success_criteria>
- Appointment page accessible and auth-protected
- All four tabs render correct components/content
- Dark navy sidebar with all specified navigation elements
- Summary bar displays booking details at a glance
- Prev/Next appointment navigation functional
- Middleware updated to protect appointment routes
</success_criteria>

<output>
After completion, create `.planning/phases/07-appointment-view-dashboard/07-02-SUMMARY.md`
</output>
