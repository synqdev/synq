---
phase: 01.1-post-mvp-ui
plan: 01
type: execute
wave: 1
depends_on: ["01-14"]
files_modified:
  - src/components/calendar/prototype-calendar-view.tsx
  - src/components/calendar/prototype-calendar-view.stories.tsx
  - src/components/ui/settings-bar.tsx
  - src/components/ui/settings-bar.stories.tsx
  - src/components/ui/timetable.tsx
  - src/components/ui/timetable.stories.tsx
  - src/components/ui/employee-timeline.tsx
  - src/components/ui/employee-timeline.stories.tsx
  - src/components/ui/employee-timeline-bar.tsx
  - src/components/ui/index.ts
autonomous: true

must_haves:
  truths:
    - "Prototype calendar view is composed from reusable UI components (SettingsBar, Timetable, EmployeeTimelineBar)"
    - "Top tabs, hourly lines (10:00-18:00), and left settings rail align to mockup layout"
    - "Timeline bars drag horizontally by 1-hour increments within bounds"
    - "Dragging prevents overlap with existing bars and prevents movement outside 10:00-18:00 window"
    - "Passed-time overlay width is computed from current time and aligned to timetable track"
  artifacts:
    - path: "src/components/ui/timetable.tsx"
      provides: "Timetable with tabs, hour-grid, drag behavior, and passed-time overlay"
      exports: ["Timetable", "TimePassedOverlay"]
    - path: "src/components/ui/settings-bar.tsx"
      provides: "Reusable settings rail component driven by settings item array"
      exports: ["SettingsBar"]
    - path: "src/components/calendar/prototype-calendar-view.tsx"
      provides: "Composed prototype built from UI components"
      exports: ["TimetableWithTabs", "SettingsRail", "TimelineBar"]
---

<objective>
Finish the post-MVP calendar prototype refactor and visual parity polish.

Purpose: Lock down reusable component boundaries and keep Storybook as the source of truth for iteration.
Output: Prototype calendar and related UI stories use the new component architecture with correct drag + layout behavior.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASE-2-FEATURE-MAP.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Componentization and story extraction</name>
  <files>
    src/components/ui/settings-bar.tsx
    src/components/ui/employee-timeline-bar.tsx
    src/components/ui/employee-timeline.tsx
    src/components/ui/timetable.tsx
    src/components/ui/*.stories.tsx
    src/components/ui/index.ts
  </files>
  <action>
Ensure component boundaries are stable:
1. SettingsBar receives an array of settings objects (`id`, `icon`, optional `label/href`).
2. Timetable receives time configuration and tab data.
3. Timetable renders employee rows from an employee data array.
4. TimePassedOverlay computes and renders passed-time coverage from current time.
5. Stories exist for each component under `UI/*` and demonstrate core behavior.
  </action>
  <verify>
`npm run build-storybook` succeeds and all `UI/SettingsBar`, `UI/EmployeeTimelineBar`, `UI/EmployeeTimeline`, and `UI/Timetable` stories render.
  </verify>
  <done>
Reusable UI components and story coverage are in place and wired through exports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Timeline interaction and visual parity pass</name>
  <files>
    src/components/ui/timetable.tsx
    src/components/ui/employee-timeline.tsx
    src/components/ui/employee-timeline-bar.tsx
    src/components/calendar/prototype-calendar-view.tsx
  </files>
  <action>
Refine interaction/layout behavior to match the calendar mockup:
1. Horizontal drag snaps in 60-minute increments from 10:00 to 18:00.
2. Drag cannot move bars outside timetable bounds.
3. Drag cannot overlap existing bars in the destination row.
4. Grid lines align with hourly boundaries across the active timeline track.
5. Booking bar radius and row track radius match so bars fully cover the gray base row.
6. Settings rail placement aligns with timetable body instead of stretching to container height.
  </action>
  <verify>
In Storybook draggable timetable story:
- Bar drag snaps hourly.
- Bar cannot cross start/end bounds.
- Bar cannot overlap another segment.
- Grid and top labels align with hour columns.
  </verify>
  <done>
Drag behavior and layout parity are functionally correct and visually consistent with the mockup.
  </done>
</task>

<task type="auto">
  <name>Task 3: Compose prototype using extracted components</name>
  <files>
    src/components/calendar/prototype-calendar-view.tsx
    src/components/calendar/prototype-calendar-view.stories.tsx
  </files>
  <action>
Keep `prototype-calendar-view` as a composed page-level wrapper over reusable components:
1. Use `Timetable` + `SettingsBar` composition only (no duplicate one-off layout logic).
2. Keep story controls/data where useful for fast iteration.
3. Keep admin glass view responsive in height for Storybook fullscreen viewport.
  </action>
  <verify>
`Calendar/NewViewPrototype` story renders using component composition and drag works in the timetable variant.
  </verify>
  <done>
Prototype page is aligned with reusable component architecture and ready for next iteration.
  </done>
</task>

</tasks>

<success_criteria>
- UI calendar prototype is componentized and maintainable.
- Drag behavior obeys hour snapping and overlap/bounds constraints.
- Storybook remains the primary preview for this workstream.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-post-mvp-ui-enhancements-prototype-calendar-data-table-updated-types-and-exports/01.1-01-SUMMARY.md`
</output>
