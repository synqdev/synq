---
phase: 03-karte-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260307_karute_rls_policies/migration.sql
  - prisma/migrations/20260307_recordings_storage_policies/migration.sql
  - src/lib/storage/recording-storage.ts
autonomous: true
user_setup:
  - service: supabase-storage
    why: "Audio recording storage bucket"
    dashboard_config:
      - task: "Create private bucket named 'recordings' with 100MB file size limit"
        location: "Supabase Dashboard -> Storage -> New bucket -> Name: recordings, Public: OFF, File size limit: 100MB, Allowed MIME types: audio/webm, audio/mp4, audio/wav, audio/ogg"

must_haves:
  truths:
    - "Four new karute tables exist in the database with proper indexes"
    - "RLS is enabled on all four karute tables with admin-only policies"
    - "Supabase Storage recordings bucket has upload/read/delete policies"
    - "Recording storage module can upload, get signed URLs, and delete audio files"
    - "Existing Customer, Worker, and Booking models have karute relation fields"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "KaruteRecord, KaruteEntry, RecordingSession, TranscriptionSegment models + KaruteStatus, RecordingStatus, KaruteEntryCategory enums"
      contains: "model KaruteRecord"
    - path: "prisma/migrations/20260307_karute_rls_policies/migration.sql"
      provides: "RLS policies for all four karute tables"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "prisma/migrations/20260307_recordings_storage_policies/migration.sql"
      provides: "Storage bucket policies for recordings"
      contains: "recordings"
    - path: "src/lib/storage/recording-storage.ts"
      provides: "Upload, signed URL, and delete functions for audio recordings"
      exports: ["uploadRecording", "getRecordingSignedUrl", "deleteRecording"]
  key_links:
    - from: "prisma/schema.prisma"
      to: "Customer model"
      via: "karuteRecords and recordingSessions relation fields on Customer"
      pattern: "karuteRecords.*KaruteRecord"
    - from: "prisma/schema.prisma"
      to: "Worker model"
      via: "karuteRecords and recordingSessions relation fields on Worker"
      pattern: "karuteRecords.*KaruteRecord"
    - from: "src/lib/storage/recording-storage.ts"
      to: "Supabase Storage"
      via: "supabase-js client with recordings bucket"
      pattern: "from\\('recordings'\\)"
---

<objective>
Prisma schema with four new karute models, RLS migration SQL, storage bucket policies, and recording storage module.

Purpose: Establish the database and storage foundation that all karute service/API code depends on.
Output: Updated schema.prisma, two SQL migration files, recording-storage.ts module.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-karte-foundation/03-CONTEXT.md
@.planning/phases/03-karte-foundation/03-RESEARCH.md
@prisma/schema.prisma
@src/lib/storage/supabase-storage.ts
@prisma/migrations/20260204_rls_policies/migration.sql
@prisma/migrations/20260219_intake_forms_storage_policies/migration.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add karute models and enums to Prisma schema</name>
  <files>prisma/schema.prisma</files>
  <action>
Add three new enums and four new models to prisma/schema.prisma. Follow the exact schema from the RESEARCH.md code examples section "Prisma Schema: Karute Models".

**Enums to add** (after existing MedicalRecordContentType enum):
- `KaruteStatus` — DRAFT, REVIEW, APPROVED
- `RecordingStatus` — RECORDING, PAUSED, COMPLETED, PROCESSING, FAILED
- `KaruteEntryCategory` — SYMPTOM, TREATMENT, BODY_AREA, PREFERENCE, LIFESTYLE, NEXT_VISIT, OTHER

**Models to add** (after existing Booking model):
- `KaruteRecord` — id, customerId (-> Customer, onDelete: Cascade), workerId (-> Worker, onDelete: Restrict), bookingId (nullable, -> Booking, onDelete: SetNull), aiSummary (nullable), status (KaruteStatus, default DRAFT), createdAt, updatedAt, entries relation, recordingSessions relation. Indexes: customerId, workerId, bookingId, status, createdAt.
- `KaruteEntry` — id, karuteId (-> KaruteRecord, onDelete: Cascade), category (KaruteEntryCategory), content (String), originalQuote (nullable), confidence (Float, default 0.0), createdAt. Indexes: karuteId, category.
- `RecordingSession` — id, karuteRecordId (nullable, -> KaruteRecord, onDelete: SetNull), workerId (-> Worker, onDelete: Restrict), customerId (-> Customer, onDelete: Cascade), bookingId (nullable, -> Booking, onDelete: SetNull), audioStoragePath (nullable), durationSeconds (nullable Int), status (RecordingStatus, default RECORDING), startedAt (default now), endedAt (nullable), createdAt. Indexes: customerId, karuteRecordId, status.
- `TranscriptionSegment` — id, recordingId (-> RecordingSession, onDelete: Cascade), segmentIndex (Int), speakerLabel (nullable), content (String), startMs (Int), endMs (Int), language (String, default "ja"), createdAt. Indexes: recordingId, compound (recordingId + segmentIndex).

**Existing models to update** (add reverse relation fields):
- `Customer`: add `karuteRecords KaruteRecord[]` and `recordingSessions RecordingSession[]`
- `Worker`: add `karuteRecords KaruteRecord[]` and `recordingSessions RecordingSession[]`
- `Booking`: add `karuteRecords KaruteRecord[]` and `recordingSessions RecordingSession[]`

After editing, run `npx prisma validate` to check schema syntax, then `npx prisma generate` to generate the client types. Do NOT run `prisma db push` (user will do this against their database).
  </action>
  <verify>Run `npx prisma validate` — must exit 0 with no errors. Run `npx prisma generate` — must exit 0.</verify>
  <done>Schema has 4 new models, 3 new enums, 3 updated models with relation fields, all indexes defined, prisma validate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create RLS and storage policy migration SQL files</name>
  <files>prisma/migrations/20260307_karute_rls_policies/migration.sql, prisma/migrations/20260307_recordings_storage_policies/migration.sql</files>
  <action>
Create two manual SQL migration files following the established pattern in `prisma/migrations/20260204_rls_policies/migration.sql` and `prisma/migrations/20260219_intake_forms_storage_policies/migration.sql`.

**File 1: `prisma/migrations/20260307_karute_rls_policies/migration.sql`**
Enable RLS on all four karute tables with admin-only policies. Follow RESEARCH.md "RLS Policy: Karute Tables" example exactly:
- `ALTER TABLE "KaruteRecord" ENABLE ROW LEVEL SECURITY;` (repeat for KaruteEntry, RecordingSession, TranscriptionSegment)
- Create `FOR ALL` policies using `current_setting('app.role', true) = 'admin'` on each table
- Use descriptive policy names: "Admin can manage karute records", "Admin can manage karute entries", etc.

**File 2: `prisma/migrations/20260307_recordings_storage_policies/migration.sql`**
Storage policies for the recordings bucket. Follow RESEARCH.md "Storage Policy: Recordings Bucket" example. Use the same idempotent `DO $$ ... END $$` block pattern from the existing intake-forms migration:
- `recordings_insert` policy — insert to authenticated, `bucket_id = 'recordings'`
- `recordings_select` policy — select to authenticated, `bucket_id = 'recordings'`
- `recordings_delete` policy — delete to authenticated, `bucket_id = 'recordings' and auth.uid() is not null`

These are manual SQL files applied outside of Prisma. They follow the project convention of `prisma db push` for schema + manual SQL for RLS/storage.
  </action>
  <verify>Both SQL files exist and contain valid SQL syntax. Verify with: `cat prisma/migrations/20260307_karute_rls_policies/migration.sql` and `cat prisma/migrations/20260307_recordings_storage_policies/migration.sql` — each should contain the expected ALTER TABLE and CREATE POLICY statements.</verify>
  <done>Two SQL migration files exist: karute RLS enables RLS on 4 tables with admin-only policies, storage policies covers insert/select/delete for recordings bucket.</done>
</task>

<task type="auto">
  <name>Task 3: Create recording storage module</name>
  <files>src/lib/storage/recording-storage.ts</files>
  <action>
Create `src/lib/storage/recording-storage.ts` mirroring the pattern in `src/lib/storage/supabase-storage.ts` but for the `recordings` bucket. Follow the RESEARCH.md "Pattern 3: Supabase Storage Module" example.

The module must:
1. Import `createClient` and `SupabaseClient` from `@supabase/supabase-js`
2. Use the same singleton pattern (`let _supabase: SupabaseClient | null = null`, `getSupabase()` function) — do NOT import from supabase-storage.ts since each module manages its own client (per existing pattern)
3. Set `const BUCKET = 'recordings'`
4. Export three functions:

`uploadRecording(recordingId: string, file: File): Promise<{ path: string }>`:
- Storage path: `${recordingId}.webm` (flat structure per user decision)
- Upload with `contentType: file.type`, `upsert: false`
- Throw on error

`getRecordingSignedUrl(path: string): Promise<string>`:
- Create signed URL with 3600 second (1 hour) expiry
- Return `data.signedUrl`
- Throw on error

`deleteRecording(path: string): Promise<void>`:
- Remove file using `remove([path])`
- Throw on error

Keep the module simple — no MIME type or size validation here (that's the API route's job, per Pattern 4 in RESEARCH.md).
  </action>
  <verify>Run `npx tsc --noEmit --pretty` — recording-storage.ts must compile without type errors.</verify>
  <done>recording-storage.ts exports uploadRecording, getRecordingSignedUrl, deleteRecording following the established supabase-storage.ts pattern.</done>
</task>

</tasks>

<verification>
1. `npx prisma validate` passes (schema is syntactically correct)
2. `npx prisma generate` passes (client types generated)
3. `npx tsc --noEmit` passes (no TypeScript errors)
4. SQL migration files exist with proper RLS and storage policies
5. recording-storage.ts follows supabase-storage.ts pattern exactly
</verification>

<success_criteria>
- Four new Prisma models with correct relations, indexes, and enums
- Customer, Worker, Booking models updated with karute relation fields
- RLS migration SQL enables row-level security on all four karute tables
- Storage policy SQL creates insert/select/delete policies for recordings bucket
- recording-storage.ts provides upload, signed URL, and delete functions
- All TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-karte-foundation/03-01-SUMMARY.md`
</output>
