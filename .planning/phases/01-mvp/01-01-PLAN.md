---
phase: 01-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
  - package.json
  - .env.example
  - src/lib/db/client.ts
autonomous: true

must_haves:
  truths:
    - "Database tables exist for Worker, Service, Resource, Customer, Booking, WorkerSchedule"
    - "Prisma can query all models without errors"
    - "Seed data creates workers, services, and resources"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "All database models with relations and indexes"
      contains: ["model Worker", "model Service", "model Resource", "model Customer", "model Booking", "model WorkerSchedule"]
    - path: "prisma/seed.ts"
      provides: "Initial seed data for development"
      min_lines: 50
    - path: "src/lib/db/client.ts"
      provides: "Singleton Prisma client"
      exports: ["prisma"]
  key_links:
    - from: "prisma/seed.ts"
      to: "prisma/schema.prisma"
      via: "Prisma client create calls"
      pattern: "prisma\\.(worker|service|resource)\\.(create|upsert)"
---

<objective>
Create the complete database schema with Prisma and Supabase, plus seed data for development.

Purpose: Foundation for all booking logic. Every feature depends on these models.
Output: Working schema with migrations applied, seed script that populates test data.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-mvp/01-RESEARCH.md

Key schema from PROJECT.md:
- Worker: id, name, nameEn, isActive
- Service: id, name, nameEn, description, duration, price, isActive
- Resource: id, name, isActive (individual beds, not capacity count)
- Customer: id, email, phone, name, locale, ticketBalance, notes
- Booking: id, createdAt, startsAt, endsAt, status, version, workerId, resourceId, customerId, serviceId
- WorkerSchedule: id, workerId, dayOfWeek, specificDate, startTime, endTime, isAvailable

Prisma + Supabase setup from RESEARCH:
- Use pgbouncer=true in connection string
- Singleton client pattern for connection pooling
- Indexes on Booking for time-based queries
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Prisma and create database schema</name>
  <files>prisma/schema.prisma, package.json, .env.example</files>
  <action>
1. Initialize Prisma if not already: `npx prisma init`
2. Create .env.example with placeholders:
   - DATABASE_URL (Supabase with pgbouncer=true)
   - DIRECT_URL (Supabase without pooler for migrations)
3. Configure prisma/schema.prisma:
   - datasource db with postgresql, url from env, directUrl for migrations
   - All 6 models from PROJECT.md schema (Worker, Service, Resource, Customer, Booking, WorkerSchedule)
   - Add composite indexes on Booking table:
     - @@index([startsAt, endsAt])
     - @@index([workerId, startsAt, endsAt])
     - @@index([resourceId, startsAt, endsAt])
   - Add indexes on WorkerSchedule:
     - @@index([workerId, dayOfWeek])
     - @@index([workerId, specificDate])
4. Add prisma scripts to package.json:
   - "db:generate": "prisma generate"
   - "db:push": "prisma db push"
   - "db:seed": "tsx prisma/seed.ts"
   - "db:reset": "prisma db push --force-reset && npm run db:seed"
5. Run `npx prisma generate` to create client types

NOTE: Do NOT run db:push yet - we need env vars configured first.
  </action>
  <verify>
- `npx prisma validate` passes without errors
- `npx prisma generate` completes successfully
- TypeScript can import PrismaClient types
  </verify>
  <done>
Schema file exists with all 6 models, proper indexes, and relations. Prisma client types generated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Prisma client singleton and seed script</name>
  <files>src/lib/db/client.ts, prisma/seed.ts</files>
  <action>
1. Create src/lib/db/client.ts with singleton pattern from RESEARCH:
   - Use globalThis to preserve client across hot reloads
   - Log queries in development only
   - Export named `prisma` client

2. Create prisma/seed.ts with initial data:
   - 3 Workers: "Tanaka", "Suzuki", "Yamamoto" (with nameEn)
   - 1 Service: "Shiatsu" / "指圧", 60 min, 6000 yen
   - 3 Resources: "Bed 1", "Bed 2", "Bed 3"
   - Default WorkerSchedule: All workers available Mon-Sat 10:00-19:00
   - Use upsert to make seed idempotent (safe to run multiple times)

3. Update package.json to add tsx as dev dependency if not present:
   - `npm install -D tsx`
  </action>
  <verify>
- TypeScript compiles src/lib/db/client.ts without errors
- prisma/seed.ts type-checks correctly
- `npx tsx --version` runs (tsx installed)
  </verify>
  <done>
Prisma client singleton at src/lib/db/client.ts. Seed script creates 3 workers, 1 service, 3 beds, and default schedules.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx prisma validate` returns no errors
2. `npx prisma generate` generates client
3. TypeScript compiles without errors: `npx tsc --noEmit`
4. All models defined: Worker, Service, Resource, Customer, Booking, WorkerSchedule
</verification>

<success_criteria>
- Prisma schema defines all 6 models with proper relations and indexes
- Singleton Prisma client prevents connection pool exhaustion
- Seed script is idempotent and creates realistic test data
- Ready for database push once env vars are configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp/01-01-SUMMARY.md`
</output>
