---
phase: 01-mvp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[locale]/layout.tsx
  - app/[locale]/page.tsx
  - app/[locale]/(public)/layout.tsx
  - app/[locale]/(user)/layout.tsx
  - app/[locale]/(admin)/layout.tsx
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - middleware.ts
  - messages/en.json
  - messages/ja.json
  - next.config.ts
  - tsconfig.json
autonomous: true

must_haves:
  truths:
    - "Routes render correctly at /en and /ja paths"
    - "Locale switching works between English and Japanese"
    - "Service layer directory structure exists"
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "next-intl routing configuration"
      contains: "defineRouting"
    - path: "middleware.ts"
      provides: "Locale detection and routing middleware"
      contains: "createMiddleware"
    - path: "app/[locale]/layout.tsx"
      provides: "Root locale layout with i18n provider"
      contains: "NextIntlClientProvider"
    - path: "messages/ja.json"
      provides: "Japanese translations"
      min_lines: 10
  key_links:
    - from: "middleware.ts"
      to: "src/i18n/routing.ts"
      via: "import routing config"
      pattern: "import.*routing.*from.*i18n/routing"
    - from: "app/[locale]/layout.tsx"
      to: "next-intl"
      via: "NextIntlClientProvider"
      pattern: "NextIntlClientProvider"
---

<objective>
Set up Next.js 15 App Router project structure with next-intl for Japanese/English locales and establish service layer directory pattern.

Purpose: Foundation for all routes and components. i18n is required for Japanese market.
Output: Working locale routing, translation files, and organized directory structure.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-mvp/01-RESEARCH.md

Project structure from RESEARCH:
- app/[locale]/(public)/ - Public routes (registration)
- app/[locale]/(user)/ - User booking flow
- app/[locale]/(admin)/ - Admin management
- src/lib/services/ - Business logic layer
- src/lib/validations/ - Zod schemas
- src/components/ui/ - Base components

next-intl setup from RESEARCH:
- Next.js 15 requires async params: `const { locale } = await params`
- Use defineRouting for route configuration
- createMiddleware for locale detection
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure next-intl with Japanese and English locales</name>
  <files>src/i18n/routing.ts, src/i18n/request.ts, middleware.ts, next.config.ts, messages/en.json, messages/ja.json</files>
  <action>
1. Install next-intl: `npm install next-intl`

2. Create src/i18n/routing.ts:
   ```typescript
   import { defineRouting } from 'next-intl/routing'

   export const routing = defineRouting({
     locales: ['ja', 'en'],
     defaultLocale: 'ja',
   })
   ```

3. Create src/i18n/request.ts:
   ```typescript
   import { getRequestConfig } from 'next-intl/server'
   import { routing } from './routing'

   export default getRequestConfig(async ({ requestLocale }) => {
     let locale = await requestLocale
     if (!locale || !routing.locales.includes(locale as any)) {
       locale = routing.defaultLocale
     }
     return {
       locale,
       messages: (await import(`../../messages/${locale}.json`)).default,
     }
   })
   ```

4. Create middleware.ts at project root:
   - Import createMiddleware from next-intl/middleware
   - Import routing from src/i18n/routing
   - Export default middleware
   - Configure matcher to exclude api, _next, _vercel, static files

5. Update next.config.ts:
   - Add withNextIntl wrapper if not using plugin approach
   - Set experimental.serverActions if needed

6. Create messages/ja.json with initial translations:
   - common: { appName: "SYNQ", loading: "読み込み中...", error: "エラー" }
   - booking: { title: "予約", selectTime: "時間を選択", confirm: "確定" }
   - admin: { title: "管理画面", workers: "スタッフ", services: "サービス" }

7. Create messages/en.json with English translations:
   - common: { appName: "SYNQ", loading: "Loading...", error: "Error" }
   - booking: { title: "Booking", selectTime: "Select time", confirm: "Confirm" }
   - admin: { title: "Admin", workers: "Workers", services: "Services" }
  </action>
  <verify>
- TypeScript compiles src/i18n/*.ts without errors
- messages/*.json are valid JSON
- `npm run build` doesn't fail on i18n config
  </verify>
  <done>
next-intl configured with ja (default) and en locales. Translation files exist with core strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create App Router directory structure with locale layouts</name>
  <files>app/[locale]/layout.tsx, app/[locale]/page.tsx, app/[locale]/(public)/layout.tsx, app/[locale]/(user)/layout.tsx, app/[locale]/(admin)/layout.tsx, tsconfig.json</files>
  <action>
1. Update tsconfig.json with path aliases:
   - "@/*": ["./src/*"]
   - "@/app/*": ["./app/*"]

2. Create app/[locale]/layout.tsx:
   - Import NextIntlClientProvider and getMessages from next-intl/server
   - Import routing from @/i18n/routing
   - Use async params (Next.js 15 pattern): `const { locale } = await params`
   - Validate locale against routing.locales, call notFound() if invalid
   - Wrap children in NextIntlClientProvider
   - Set html lang attribute to locale
   - Add basic Tailwind body styles

3. Create app/[locale]/page.tsx:
   - Simple redirect to /(public)/register or show landing
   - Use getTranslations for server-side i18n

4. Create app/[locale]/(public)/layout.tsx:
   - Minimal layout for public pages (registration, landing)
   - Pass through children with minimal wrapper

5. Create app/[locale]/(user)/layout.tsx:
   - Layout for authenticated user pages (booking)
   - Will add auth check later (Plan 07)

6. Create app/[locale]/(admin)/layout.tsx:
   - Layout for admin pages
   - Will add auth middleware later (Plan 09)

7. Create placeholder pages:
   - app/[locale]/(public)/page.tsx - "Welcome" placeholder
   - app/[locale]/(user)/booking/page.tsx - "Booking" placeholder
   - app/[locale]/(admin)/dashboard/page.tsx - "Admin" placeholder

8. Create service layer directories (empty, structure only):
   - src/lib/services/.gitkeep
   - src/lib/validations/.gitkeep
   - src/lib/utils/.gitkeep
   - src/components/ui/.gitkeep
   - src/components/calendar/.gitkeep
   - src/types/.gitkeep
   - src/hooks/.gitkeep
  </action>
  <verify>
- `npm run dev` starts without errors
- http://localhost:3000/ja shows Japanese locale
- http://localhost:3000/en shows English locale
- Route groups (public), (user), (admin) don't appear in URL
  </verify>
  <done>
App Router structure with locale routing, route groups, and empty service layer directories. Pages render at /ja and /en.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run dev` starts successfully
2. Visit /ja - shows Japanese content
3. Visit /en - shows English content
4. Visit / - redirects to /ja (default locale)
5. Directory structure matches RESEARCH recommended pattern
</verification>

<success_criteria>
- next-intl properly configured with ja/en locales
- All route groups exist: (public), (user), (admin)
- Service layer directories created for future plans
- TypeScript path aliases working
- No console errors in development
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp/01-02-SUMMARY.md`
</output>
