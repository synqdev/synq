---
phase: 07-appointment-view-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/[locale]/(admin)/admin/dashboard/prototype-client.tsx
  - src/components/appointment/TodayAppointments.tsx
  - src/components/appointment/AppointmentCard.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard has a 'Today' tab that shows today's appointments as a vertical card list"
    - "Each card shows customer name, time, service, and karute status"
    - "Each card has quick actions: Open appointment, Start recording, View karute, Customer profile"
    - "Empty state shows a message when no appointments exist for today"
  artifacts:
    - path: "src/components/appointment/TodayAppointments.tsx"
      provides: "Container component fetching and rendering today's appointments"
      min_lines: 40
    - path: "src/components/appointment/AppointmentCard.tsx"
      provides: "Individual appointment card with quick actions"
      min_lines: 40
    - path: "app/[locale]/(admin)/admin/dashboard/prototype-client.tsx"
      provides: "Updated dashboard with 'today' tab in tabs array and embeddedPanelTabs"
      contains: "today"
  key_links:
    - from: "src/components/appointment/TodayAppointments.tsx"
      to: "/api/admin/appointment/today"
      via: "SWR fetch for today's bookings"
      pattern: "useSWR.*appointment/today"
    - from: "app/[locale]/(admin)/admin/dashboard/prototype-client.tsx"
      to: "src/components/appointment/TodayAppointments.tsx"
      via: "panelContent conditional rendering"
      pattern: "TodayAppointments"
    - from: "src/components/appointment/AppointmentCard.tsx"
      to: "appointment/[id]"
      via: "Link to appointment workstation"
      pattern: "Link.*appointment"
---

<objective>
Add a "Today" tab to the existing admin dashboard that shows today's appointments as a vertical list of cards with quick actions (open appointment, start recording, view karute, customer profile).

Purpose: Gives practitioners a quick overview of their day and one-click access to appointment workstation, recording, and karute for each booking.
Output: TodayAppointments container, AppointmentCard component, updated dashboard tabs.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-appointment-view-dashboard/07-CONTEXT.md
@.planning/phases/07-appointment-view-dashboard/07-RESEARCH.md
@.planning/phases/07-appointment-view-dashboard/07-01-SUMMARY.md
@app/[locale]/(admin)/admin/dashboard/prototype-client.tsx
@src/components/ui/card.tsx
@src/components/ui/button.tsx
@messages/en.json
@messages/ja.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppointmentCard and TodayAppointments components</name>
  <files>
    src/components/appointment/AppointmentCard.tsx
    src/components/appointment/TodayAppointments.tsx
  </files>
  <action>
1. Create `src/components/appointment/AppointmentCard.tsx`:
   - 'use client' component
   - Props: booking object (id, startsAt, endsAt, customer: { id, name }, service: { name, nameEn }, worker: { name }, karuteRecords: Array<{ id, status }>), locale (string)
   - Layout: A card (rounded-xl border border-gray-200 bg-white p-4 shadow-sm) with two rows:
     a. Top row (flex items-center gap-4):
        - Time block (text-right, min-w-[60px]): formatted start time (HH:MM) in font-semibold text-sm, end time in text-xs text-gray-500
        - Customer and service: customer.name in font-medium text-gray-900, service.name below in text-sm text-gray-500
        - Karute status badge: If karuteRecords.length > 0, show green pill (bg-green-100 text-green-700) with i18n key `admin.today.hasKarute`; otherwise gray pill (bg-gray-100 text-gray-500) with `admin.today.noKarute`
     b. Bottom row (flex gap-2 mt-3 border-t border-gray-100 pt-3):
        - "Open" button: Link to `/${locale}/appointment/${booking.id}`, use Button variant="outline" size="sm", label from `admin.today.open`
        - "Record" button: Link to `/${locale}/appointment/${booking.id}?tab=recording`, Button variant="ghost" size="sm" with mic SVG icon, label from `admin.today.startRecording`
        - "Karute" button: Link to `/${locale}/appointment/${booking.id}?tab=karute`, Button variant="ghost" size="sm" with clipboard SVG icon, label from `admin.today.viewKarute`
        - "Profile" button: Link to `/${locale}/admin/customers/${booking.customer.id}`, Button variant="ghost" size="sm" with user SVG icon, label from `admin.today.customerProfile`
   - Use useTranslations('admin.today') for all labels
   - Format times using formatInTimeZone from '@/lib/utils/time' (follow existing codebase pattern)
   - Use inline SVG icons (no icon library -- consistent with codebase). Keep icons simple: 16x16 or 20x20 viewBox.

2. Create `src/components/appointment/TodayAppointments.tsx`:
   - 'use client' component
   - Props: locale (string), dateStr (string) -- the currently active date from the dashboard
   - Use SWR to fetch from `/api/admin/appointment/today?date=${dateStr}` with { revalidateOnFocus: false } (consistent with codebase SWR usage)
   - Loading state: Show Spinner component centered
   - Error state: Show error message
   - Empty state: Show centered text with `admin.today.noAppointments` i18n key and a calendar SVG icon above it
   - Success state: Render a vertical list of AppointmentCard components with gap-3, wrapped in a max-w-2xl mx-auto container for comfortable reading width
   - Use useTranslations('admin.today') for the title header
   - Show a header: `<h2 className="text-lg font-semibold text-gray-900 mb-4">{t('title')}</h2>`
  </action>
  <verify>
    `npm run build` succeeds without TypeScript errors.
    Components export correctly and can be imported.
  </verify>
  <done>
    TodayAppointments fetches today's bookings via SWR and renders AppointmentCard list. Each card shows customer, time, service, karute status, and four quick action buttons. Empty state handled gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Today tab into dashboard prototype-client</name>
  <files>
    app/[locale]/(admin)/admin/dashboard/prototype-client.tsx
  </files>
  <action>
1. Add import at top of prototype-client.tsx:
   ```
   import { TodayAppointments } from '@/components/appointment/TodayAppointments'
   ```

2. Add 'today' tab to the `tabs` array. Insert it as the SECOND item (after 'dashboard', before 'schedule'):
   ```
   { id: 'today', label: 'Today', icon: 'calendar' }
   ```
   Note: The icon value 'calendar' should work with the existing icon rendering in TimetableWithTabs. If TimetableWithTabs uses a different icon mechanism, match the existing pattern exactly.

3. Add 'today' to the `embeddedPanelTabs` Set:
   ```
   const embeddedPanelTabs = new Set(['schedule', 'workers', 'services', 'resources', 'customers', 'reports', 'today'])
   ```

4. Add the today panel content to the `panelContent` conditional chain. Add it BEFORE the final `: undefined`. Insert:
   ```
   : activeTabId === 'today' ? (
     <div className="h-full overflow-auto p-4">
       <TodayAppointments locale={locale} dateStr={activeDateStr} />
     </div>
   )
   ```

5. Do NOT modify any other existing tab behavior or panel content. Only add the new 'today' tab.
  </action>
  <verify>
    `npm run build` succeeds.
    Navigate to dashboard, click "Today" tab -- should show TodayAppointments component with cards or empty state.
  </verify>
  <done>
    Dashboard has a "Today" tab. Clicking it shows today's appointments as cards with quick actions. All existing tabs still work.
  </done>
</task>

</tasks>

<verification>
- Dashboard loads with "Today" tab visible in tab bar
- Clicking "Today" tab shows appointment cards or empty state
- Each card shows customer name, time, service, karute status
- Quick action buttons link to correct appointment page URLs
- All existing dashboard tabs continue to work
- `npm run build` succeeds
</verification>

<success_criteria>
- "Today" tab appears in dashboard tab bar
- Today's appointments render as vertical card list
- Each card has four quick actions (open, record, karute, profile)
- Karute status visually distinguishes "has karute" vs "no karute"
- Empty state message shown when no appointments for the day
</success_criteria>

<output>
After completion, create `.planning/phases/07-appointment-view-dashboard/07-03-SUMMARY.md`
</output>
