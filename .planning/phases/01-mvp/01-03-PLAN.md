---
phase: 01-mvp
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/card.tsx
  - src/components/ui/spinner.tsx
  - src/components/ui/index.ts
  - tailwind.config.ts
  - app/globals.css
autonomous: true

must_haves:
  truths:
    - "Button component renders with multiple variants"
    - "Input component handles focus and error states"
    - "Card component wraps content with consistent styling"
    - "Components use Tailwind classes, no external UI library"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Reusable Button component"
      exports: ["Button"]
      min_lines: 20
    - path: "src/components/ui/input.tsx"
      provides: "Reusable Input component"
      exports: ["Input"]
    - path: "src/components/ui/card.tsx"
      provides: "Reusable Card component"
      exports: ["Card"]
    - path: "src/components/ui/index.ts"
      provides: "Barrel export for all UI components"
      contains: "export"
  key_links:
    - from: "src/components/ui/*.tsx"
      to: "tailwind.config.ts"
      via: "Tailwind design tokens"
      pattern: "className.*bg-|text-|border-"
---

<objective>
Create foundational UI component library with Button, Input, Card, and Spinner using Tailwind CSS (no external component library).

Purpose: Consistent styling across user and admin interfaces. Custom components = leaner bundle.
Output: Reusable, typed components with variant support.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-mvp/01-RESEARCH.md

Decision from PROJECT.md:
- Custom UI components over Shadcn (leaner bundle, full control)
- Tailwind CSS with design tokens for consistency

Component requirements:
- Button: primary, secondary, outline, ghost variants; sm, md, lg sizes; loading state
- Input: label, error message, focus ring, disabled state
- Card: header, body sections; shadow, border options
- Spinner: loading indicator for async operations
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Tailwind with design tokens</name>
  <files>tailwind.config.ts, app/globals.css</files>
  <action>
1. Update tailwind.config.ts with custom theme:
   - colors: Add brand colors (primary, secondary) with shade variations (50-950)
     - primary: Blue-ish for actions (#3B82F6 base)
     - secondary: Gray for secondary actions
     - success: Green for confirmations
     - error: Red for errors/warnings
   - fontFamily: Add Japanese-friendly font stack (system-ui, "Hiragino Sans", "Yu Gothic", sans-serif)
   - extend spacing if needed for calendar grid
   - Add keyframes for spinner animation

2. Update app/globals.css:
   - Import Tailwind base, components, utilities
   - Add CSS custom properties for theme colors (allows runtime theming later)
   - Add base styles for Japanese text rendering
   - Add focus-visible utilities for accessibility
  </action>
  <verify>
- `npm run dev` compiles Tailwind without errors
- Custom colors available in class names (e.g., bg-primary-500)
- Japanese fonts render correctly
  </verify>
  <done>
Tailwind configured with brand colors, Japanese font stack, and spinner animation keyframes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Button, Input, Card, and Spinner components</name>
  <files>src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/card.tsx, src/components/ui/spinner.tsx, src/components/ui/index.ts</files>
  <action>
1. Create src/components/ui/button.tsx:
   - Props: variant (primary | secondary | outline | ghost), size (sm | md | lg), loading, disabled, className, children
   - Use React.forwardRef for proper ref handling
   - Combine class names with clsx or template literals
   - Show Spinner when loading, disable button
   - TypeScript: Extend ButtonHTMLAttributes<HTMLButtonElement>

2. Create src/components/ui/input.tsx:
   - Props: label, error, className, and standard input props
   - Use React.forwardRef
   - Wrapper div with label above input
   - Error message below input (red text)
   - Focus ring using focus-visible:ring-2
   - TypeScript: Extend InputHTMLAttributes<HTMLInputElement>

3. Create src/components/ui/card.tsx:
   - Props: title (optional), children, className
   - Header section if title provided
   - Body section with padding
   - Shadow and border styling
   - Compound component pattern optional (Card.Header, Card.Body)

4. Create src/components/ui/spinner.tsx:
   - Props: size (sm | md | lg), className
   - Animated SVG or div with Tailwind animation
   - Use the keyframes defined in tailwind.config.ts
   - Accessible: aria-label="Loading"

5. Create src/components/ui/index.ts barrel export:
   - export { Button } from './button'
   - export { Input } from './input'
   - export { Card } from './card'
   - export { Spinner } from './spinner'

NOTE: All components are 'use client' since they handle interactive states.
  </action>
  <verify>
- TypeScript compiles all component files without errors
- Import from '@/components/ui' works (barrel export)
- Components render in a test page without errors
  </verify>
  <done>
Four UI components (Button, Input, Card, Spinner) with TypeScript types, variants, and proper ref forwarding.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. All components export correctly from src/components/ui/index.ts
2. TypeScript compiles without errors: `npx tsc --noEmit`
3. Components can be imported and used in any page
4. Button variants visually distinct (primary blue, outline bordered, etc.)
</verification>

<success_criteria>
- Button: 4 variants, 3 sizes, loading state works
- Input: Label displays, error shows in red, focus ring visible
- Card: Clean container with optional title
- Spinner: Animates smoothly, accessible
- No external UI library dependencies (custom Tailwind only)
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp/01-03-SUMMARY.md`
</output>
